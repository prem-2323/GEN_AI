"""text — shared text services for the image & video generation pipelines.

Modules:
    qwen_service        — Ollama Qwen text generation (single entry point)
    document_extractor  — TXT / PDF / DOCX text extraction from uploads
    tts                 — Edge TTS narration audio generation
"""
